diff --git a/node_modules/rehype-pretty-code/dist/index.js b/node_modules/rehype-pretty-code/dist/index.js
index cae4b6f..4c11deb 100644
--- a/node_modules/rehype-pretty-code/dist/index.js
+++ b/node_modules/rehype-pretty-code/dist/index.js
@@ -1,4 +1,3 @@
-import { getSingletonHighlighter } from 'shiki';
 import { visit } from 'unist-util-visit';
 import { toString } from 'hast-util-to-string';
 import rangeParser2 from 'parse-numeric-range';
@@ -468,6 +467,16 @@ function apply(element, {
 }
 var globalHighlighterCache = /* @__PURE__ */ new Map();
 var hastParser = unified().use(rehypeParse, { fragment: true });
+function requireGetHighlighter() {
+  throw new Error(
+    "rehype-pretty-code: no `getHighlighter` option was provided. The default " +
+      "`shiki` singleton-highlighter fallback was removed by a local patch " +
+      "(patches/rehype-pretty-code+0.14.1.patch, SPEC-MAENGV2-EDITOR-MERGE-006 " +
+      "AC-M4-006) to keep the full `shiki` bundled language set (200+ grammars + " +
+      "the oniguruma WASM engine) out of the Cloudflare Worker bundle. Pass an " +
+      "explicit `getHighlighter` option (see apps/maeng-blog/src/lib/content/render.ts)."
+  );
+}
 var src_default = rehypePrettyCode;
 function rehypePrettyCode(options = {}) {
   const {
@@ -478,7 +487,7 @@ function rehypePrettyCode(options = {}) {
     defaultLang = "",
     tokensMap = {},
     filterMetaString = (v) => v,
-    getHighlighter = getSingletonHighlighter,
+    getHighlighter = requireGetHighlighter,
     transformers,
     onVisitLine,
     onVisitHighlightedLine,
